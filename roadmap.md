# Roadmap

- [ ] Identificação humana na lista de pedidos de mercadoria (view `vw_compra_pedido_identidade`): coluna Número com competência + tooltip, referências alternativas, busca pelo campo `busca`, ordenação por competência.
- [ ] Criar atribuição direto do passo do processo: diálogo com criação via `fn_atribuicao_salvar` + vínculo, aviso quando `quem_executa` ≠ 'time', seletor de `quem_executa` no card do passo.
