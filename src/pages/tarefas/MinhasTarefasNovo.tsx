/**
 * Placeholder da fatia F1 — a lista completa de "Minhas Tarefas" é F2.
 * A tela padrão do módulo é Hoje.
 */
export default function MinhasTarefasNovo() {
  return (
    <div className="mx-auto w-full max-w-3xl space-y-2 p-6">
      <h1 className="text-2xl font-bold tracking-tight">Minhas Tarefas</h1>
      <p className="text-sm text-muted-foreground">
        Lista completa com filtros e agrupamentos chega na próxima fatia. Use a aba Hoje.
      </p>
    </div>
  );
}
