Verificação concluída, sem alterações. Typecheck `bunx tsgo --noEmit` retornou exit 0 sem saída — zero erros. Nenhum erro nos quatro arquivos citados. Os avisos `TS2589` em `useBaixasPendentes.ts` e `BancoSafra.tsx` que o harness reportou não se reproduzem no tsgo. Nada a corrigir.